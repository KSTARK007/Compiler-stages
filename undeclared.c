#include <stdio.h>

using system;

class drogram
{
	n = 37; //check if n is prime
	int i = 4.3;
	int factors = 0;
	for(i = 1; i <= n; i++)
	{
        i = i + 1;
        i = i + 3;
	}
	if(i < n)
	{	i = i + 1;
	    factors = factors + 2;
	}
	while(i < n)
	{
		factors = factors + h;
	}
}
